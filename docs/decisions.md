# Decisions

Status: active register. Owner: Nate Kramber. Started 2026-09-12. Written in ASD-STE100.

This file records each owner decision. Each decision has an id (D-#). The numbers never change. A changed decision stays in this file, and its Effect column names the later decision. `.claude/rules/registers.md` holds the rules of this file.

How to read this file:

- The Decision column gives the answer.
- The Effect column gives what the decision changes, binds, or supersedes.
- A date in the Effect column marks a later revision.

## Project

| Id | Date | Topic | Decision | Effect |
|---|---|---|---|---|
| D-1 | 2026-09-12 | Reference repositories | Use decktome and What You Carry as the model for `CLAUDE.md`, skills, agents, rules, and the other docs. A separate Codex review after a code pull request is not a rule here. Follow decktome for review. | The rules that both repositories share apply here: a branch for each change, the owner merges, ask questions at once, push back with evidence, other repositories read-only, and a date on each external fact. |
| D-2 | 2026-09-12 | First projects | Deck Tome and What You Carry are the first projects. The project cards are reusable, so a new project such as terminal-rpg is easy to add. | One card component and one data entry for each project (G-2). The `add-project` skill holds the procedure. |
| D-3 | 2026-09-12 | Domain | The owner bought `natekramber.com`. GoDaddy is the registrar. | DNS starts at GoDaddy. The hosting question (OQ-2) reads this. |
| D-4 | 2026-09-12 | Site shape | A single-page layout on current design trends. A maximum focus on responsive layout, simple and readable code, and best practices. | Sets tenets T-1 and T-3. The roadmap questions set the details. |

## Repository and process

| Id | Date | Topic | Decision | Effect |
|---|---|---|---|---|
| D-5 | 2026-09-12 | Review | Gitar is the only reviewer. Every pull request waits for its review, documents alone included. When Gitar pauses automatic reviews and posts no review, comment `Gitar review` on the pull request. The owner states that the Gitar app covers every repository of the owner. | Hard rule 4 and the `gitar-review` skill. |
| D-6 | 2026-09-12 | Attribution | No AI attribution in any file, commit, pull request, branch name, or comment. | Tenet T-6 and hard rule 6. `.claude/settings.json` sets both attribution strings empty. Revised in part by D-26 on 2026-09-12: the site copy can describe the AI workflow. The rest stands. |
| D-7 | 2026-09-12 | Writing standard | ASD-STE100 for every doc, skill, agent, and rule file, with a checker. The words a visitor reads on the site are exempt. | Hard rule 5, the `ste-writing` skill, `scripts/ste-check.py`, and the `verify:docs` job. |
| D-8 | 2026-09-12 | Session handoff | The hybrid format: a "Resume here" block, then numbered session entries in six parts. The file keeps the ten newest entries, and the archive keeps the rest. | The `session-handoff` skill. |
| D-9 | 2026-09-12 | Agent file | `AGENTS.md` is a symlink to `CLAUDE.md`. | One source of truth. Edit `CLAUDE.md` alone. |
| D-10 | 2026-09-12 | Planning docs | The trimmed design template: status, thesis, tenets, guardrails, roadmap, and sequence in `docs/design.md`. Decisions in `docs/decisions.md`, questions in `docs/questions.md`. Lessons, findings, and a cost model join when an entry fills them. | The `design-doc-style` skill. |
| D-11 | 2026-09-12 | Git guards | Squash merge alone, automatic delete of a merged branch, and a `main` ruleset that requires a pull request and refuses a force push and a delete. Each required check joins the ruleset after its first run. The owner did not choose a pre-commit hook, a `where` script, commit prefixes, or a pull request template. | GitHub settings changed 2026-09-12. Ruleset `main`, id 23087504. `verify:docs` joined the ruleset as a required check on 2026-09-12, after the merge of PR #1. `verify:site` joined it on 2026-09-13 UTC, after the merge of PR #3. The four `verify:site-*` checks joined it on 2026-09-13 UTC, after the merge of PR #4. `verify:site-preview` joined it on 2026-09-14 UTC, before the merge of PR #10 (D-81). |
| D-12 | 2026-09-12 | Pull requests per session | One concern per pull request. No limit per session. | Hard rule 3. |
| D-13 | 2026-09-12 | Skills | `ste-writing`, `design-doc-style`, `gitar-review`, `add-project`, `responsive-qa`, and `session-handoff`. | `.claude/skills/`. |
| D-14 | 2026-09-12 | Agents | `project-researcher`, `responsive-auditor`, `accessibility-auditor`, and `copy-editor`. | `.claude/agents/`. |
| D-15 | 2026-09-12 | Rule style | Ranked tenets and numbered hard rules. The owner sets the order and the words of the tenets in the roadmap questions. | `CLAUDE.md`. OQ-1. D-39 sets the order. |
| D-16 | 2026-09-12 | Upkeep | Dependabot opens one grouped pull request for each ecosystem each month. Every action pins a commit SHA. One local command runs every check. A weekly workflow checks every outbound link. | `.github/dependabot.yml` and `make verify`. The link check arrives with the site, because the site holds the links. |
| D-17 | 2026-09-12 | Rule location | `CLAUDE.md` holds the tenets, the hard rules, and the read order. Rules for one area live in `.claude/rules/`, scoped by path. | `CLAUDE.md` lists each rule file. |
| D-18 | 2026-09-12 | License | MIT for the code. The site text, photos, and images stay all rights reserved. | `LICENSE` and the License section of `README.md`. |

## Site

| Id | Date | Topic | Decision | Effect |
|---|---|---|---|---|
| D-19 | 2026-09-12 | Site goal | Personal brand and projects. The site is for peers and the curious. It leads with who the owner is and what the owner makes, with no hard sell. | Sets the order of the page: hero, About, projects, and links. |
| D-20 | 2026-09-12 | Visitor action | Links to GitHub (`github.com/nkramber`) and LinkedIn (`linkedin.com/in/nate-kramber`). No other social profile, and no email on the page. | The hero and the links section. A profile URL is not personal data under hard rule 10. Revised in part by D-102 on 2026-09-14: the hero alone shows the links until the project cards arrive. The rest stands. |
| D-21 | 2026-09-12 | Sections | Hero, About, Projects, and Links. The owner did not choose an experience timeline, a skills list, or a resume download. | The page structure in `docs/design.md`. Revised in part by D-102 on 2026-09-14: the Links section waits for the project cards. The rest stands. |
| D-22 | 2026-09-12 | Card fields | Every card has a title, a one-line pitch, links, a status badge, stack tags, highlights, and one screenshot. The owner did not choose a year field. | The project schema (G-2). A missing field fails the build (D-30). |
| D-23 | 2026-09-12 | Card behavior | A card expands in place with a native disclosure element. An order number in each entry, set by the owner, sorts the cards. | No dialog, and no client JavaScript for the cards (D-33). |
| D-24 | 2026-09-12 | Deck Tome card | The card name is "Deck Tome", two words. The owner chose it over decktome D-556, which names the product "Decktome". The card links to `decktome.com` and the public repository, with an "Invite only" label. | D-2 stands. decktome D-310 keeps the app invite-only (read 2026-09-12). On 2026-09-14, D-310 still reads "No public sign-up", and decktome D-577 of 2026-09-07 names the product "Deck Tome" too, so the card name and the app name agree. PR-10 applies this decision with D-116. |
| D-25 | 2026-09-12 | What You Carry card | The public visibility of the repository is intended, and the card links to it. The card does not mention Steam or any release plan. | What You Carry D-106 still reads "Private until launch" (read 2026-09-12). The owner records the change in that repository. |
| D-26 | 2026-09-12 | AI workflow on the site | The site copy describes openly how the owner directs AI coding agents. | Revises D-6 in part: the no-attribution rule covers commits, pull requests, branch names, comments, and code. Tenet T-6 changes to match. |
| D-27 | 2026-09-12 | Visual direction | An editorial minimal layout: an oversized headline, whitespace, and one column of project rows. The color scheme follows the system setting, with no toggle. A sans for text and a monospace for tags, badges, and labels, both self-hosted. Subtle CSS motion that stops under reduced motion. | The page shell and the design tokens. |
| D-28 | 2026-09-12 | Voice and About | A plain and direct voice: short sentences, concrete verbs, first person, and no buzzwords. The owner writes the bio. The About section has no image. | The `copy-editor` agent applies the voice. OQ-3 tracks the bio. Revised in part by D-94 on 2026-09-14: the owner gives the facts in an interview, the session drafts the bio from them, and the owner approves the words. The rest stands. |
| D-29 | 2026-09-12 | Headline | Draft B: "I design the systems. AI agents write the code. I decide what ships." The second line: "Tools and games, built under strict rules and real review." | The hero. It applies D-26. |
| D-30 | 2026-09-12 | Stack | Astro 7 static output. A content collection with a schema holds the project entries, and the Astro image components serve the screenshots. | Astro 7 needs Node 22.12 or newer (read 2026-09-12). The stack pull request pins the Node version. D-44 pins Node 22.23.2. |
| D-31 | 2026-09-12 | CSS | Modern vanilla CSS with custom properties as design tokens, native nesting, container queries, and `:has()`. | No CSS dependency. |
| D-32 | 2026-09-12 | Browser support | Every essential feature is Baseline Widely available. A Baseline Newly available feature adds polish alone, with a fallback. A Baseline Limited feature stays out. | A guardrail in `docs/design.md`. |
| D-33 | 2026-09-12 | JavaScript | Zero client JavaScript by default. Each script needs its own decision that names its purpose. | A guardrail in `docs/design.md`. D-36 needs no script. |
| D-34 | 2026-09-12 | Hosting | Firebase Hosting (classic) in a new, dedicated Google Cloud project on the Spark plan, with no billing account. | Closes OQ-2. DNS stays at GoDaddy with a TXT record and an A record. M-2 checks the log link on Spark. |
| D-35 | 2026-09-12 | Deploy | Each merge to `main` deploys from GitHub Actions through Workload Identity Federation, with no key. Only `main` reaches production. A pull request gets a preview only if a keyless preview deploy works. | M-1 tests the keyless preview. If it fails, the owner decides. |
| D-36 | 2026-09-12 | Analytics | Visit counts come from the Hosting request logs in Cloud Logging. No script and no cookies. | Keeps D-33. M-2 checks the log link. |
| D-37 | 2026-09-12 | Performance budget | Core Web Vitals "good" (LCP 2.5 s, INP 200 ms, CLS 0.1, at the 75th percentile), Lighthouse 95 or more in every category on mobile, 0 KB of client JavaScript, and a first-load weight cap that the stack pull request sets. CI enforces the budget. | Tenet T-4. The web.dev thresholds were read 2026-09-12. D-48 sets the weight cap. |
| D-38 | 2026-09-12 | Site checks | Every pull request that changes the site passes four checks: Playwright responsive tests at every `responsive-qa` width, an axe scan with the cards closed and open, Lighthouse CI against D-37, and HTML validation with an internal link check. | Each check joins the `main` ruleset after its first green run (D-11). Revised in part by D-50 on 2026-09-12: a Lighthouse 13 script replaces Lighthouse CI. The other three checks stand. |
| D-39 | 2026-09-12 | Tenet order | Keep the draft order: T-1 Responsive first, T-2 Accessible, T-3 Simple and readable code, T-4 Fast, T-5 Document everything. T-6 stays absolute. | Closes OQ-1. |
| D-40 | 2026-09-12 | Launch | A placeholder page goes live as soon as the stack exists. No launch deadline. The cards launch with designed placeholder images, and real screenshots replace them later. | OQ-4 tracks the screenshots. |
| D-41 | 2026-09-12 | Defaults | Keep the defaults of the session: npm as the package manager, `natekramber.com` as the main address with `www` redirected to it, security headers in `firebase.json`, a social preview image and a meta description, a custom 404 page, the Wizards of the Coast fan content line when a screenshot shows card art, and a placeholder page with the headline and the two links. | PR-3, PR-5, PR-6, PR-8, and PR-10 in `docs/design.md`. |
| D-42 | 2026-09-12 | Fonts and accent color | The owner picks the fonts and the accent color from a rendered preview page: three font pairings and three accent colors, in light and dark, at phone and desktop widths. | PR-7. OQ-5 holds the pick. |
| D-43 | 2026-09-12 | Card order | Deck Tome is the first card, and What You Carry is the second. | The order numbers of PR-10 and PR-11. |
| D-44 | 2026-09-12 | Node version | Pin Node 22.23.2, the newest Node 22 LTS release, in `.nvmrc`. The owner chose it over Node 24 LTS. | PR-3. Astro 7.3.2 needs Node 22.12.0 or newer. Node 22 entered maintenance on 2025-10-21 and reaches its end of life on 2027-04-30, so a bump must come before that date (nodejs/Release schedule, read 2026-09-12). |
| D-45 | 2026-09-12 | Astro telemetry | Turn off the anonymous usage data of Astro for this repository. | The Makefile exports `ASTRO_TELEMETRY_DISABLED=1`, and CI runs every Astro command through `make`. Astro documents the variable at https://astro.build/telemetry/ (read 2026-09-12). The first local build and preview of PR-3 ran before this decision, with telemetry on. |
| D-46 | 2026-09-12 | HTML validator | `html-validate` (11.15.0 on 2026-09-12) checks the built HTML. It runs offline in JavaScript on Node 22, with no Java. | PR-4. The owner chose it over the Nu Html Checker, which needs Java 17. |
| D-47 | 2026-09-12 | Link checker | `linkinator` (8.1.0 on 2026-09-12) checks the internal links and the `#` anchors of the built site. | PR-4. PR-12 can use the same tool for the outbound links. |
| D-48 | 2026-09-12 | Page weight cap | The first mobile load stays at or under 300 KB (300,000 bytes), as the Lighthouse resource summary counts it. | Completes D-37. Chrome's docs advise a total under 1,600 KiB (read 2026-09-12). |
| D-49 | 2026-09-12 | Lighthouse runner | Keep Lighthouse CI (`@lhci/cli` 0.15.1) with the median of 3 runs. Its latest release came out on 2025-06-25, and it runs Lighthouse 12.6.1 while Lighthouse 13.4.1 is current (npm registry, read 2026-09-12). | PR-4. A later decision can move to Lighthouse 13 if Lighthouse CI falls further behind. Superseded by D-50 on 2026-09-12. |
| D-50 | 2026-09-12 | Lighthouse runner, revised | Remove Lighthouse CI. A short script runs Lighthouse 13.4.1 three times on mobile, takes the median, and checks the budget of D-37 and D-48. | Supersedes D-49. The install of Lighthouse CI 0.15.1 added 12 npm audit advisories (7 high), all from its own dependency tree. Lighthouse 13.4.1 alone audits with 0 advisories and supports Node 22.23.2 (npm registry and npm audit, read 2026-09-12). |
| D-51 | 2026-09-12 | Google Cloud project id | The project id of the site is `natekramber-prod`. | PR-5. A project id is global and permanent. If the id is taken, the owner picks another. Closes OQ-6. |
| D-52 | 2026-09-12 | Google account | The account that owns decktome-prod also owns `natekramber-prod`. PR-5 creates a gcloud configuration named `natekramber` for that account and project. | PR-5. The configuration keeps a command off decktome-prod and off every other project (decktome handoff, 2026-09-10). The account address stays out of the repository (hard rule 10). Closes OQ-7. |
| D-53 | 2026-09-12 | Firebase CLI install | A separate npm project in `deploy/`, with its own `package.json` and lockfile, holds `firebase-tools` alone. Only the deploy jobs install it. | PR-5. firebase-tools 15.30.0 brings 674 packages, 257 MB, and 9 moderate npm audit advisories, and it ships no shrinkwrap (npm registry and npm audit, read 2026-09-12). The root audit stays at 0 (compare D-50), and Dependabot gets an npm entry for `/deploy`. The owner chose it over a pinned `npx` command (the decktome pattern) and a root devDependency. |
| D-54 | 2026-09-12 | Project selection | The repository has no `.firebaserc`. Each workflow step and each command in `docs/deploy.md` names its project with `--project`. | PR-5. A command with no project fails and does not pick one, the same guard as D-52. The Firebase CLI docs advise against a committed `.firebaserc` in an open-source repository (https://firebase.google.com/docs/cli, read 2026-09-12). |
| D-55 | 2026-09-12 | Repository location | Move this repository from `/Users/nate/Repos/portfolio` to `/Volumes/SSD-1TB/portfolio`, next to What You Carry. Do not move it in Session 6. The move is the first action of the next session. | The Session 6 entry of `docs/session-handoff.md` held the checklist. After the move, `CLAUDE.md` names the new path, and the CAUTION about the external drive covers this repository too. Superseded by D-69 on 2026-09-13, which removed the checklist from the handoff. |
| D-56 | 2026-09-12 | Preview project | A second Spark project, `natekramber-preview`, holds the preview channels. Its deploy service account has no role in `natekramber-prod`. | Revises D-34 in part: the site uses two dedicated projects. The rest of D-34 stands. One role, `roles/firebasehosting.admin`, permits both a preview deploy and a live release through `firebasehosting.sites.update`, and Hosting has no IAM policy on one site (Google Cloud IAM docs and firebase-tools 15.30.0 source, read 2026-09-12). So IAM itself enforces G-10. If the id is taken, the owner picks another. |
| D-57 | 2026-09-12 | Content Security Policy | Set `build.inlineStylesheets` to `'never'`. `firebase.json` sends one strict CSP header: `default-src 'none'; connect-src 'self'; img-src 'self' data:; style-src 'self'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'`. | PR-5. A header CSP with `style-src 'self'` blocked the inline style of the placeholder, and Lighthouse Best Practices fell to 0.92 (local probe, Lighthouse 13.4.1, 2026-09-12). The external CSS cost about 150 ms of simulated FCP, and Performance stayed 1. `connect-src 'self'` keeps the robots.txt and llms.txt fetches of Lighthouse. `data:` serves the placeholder favicon until PR-8. `default-src 'none'` also blocks every script, a second guard for G-5. The owner chose it over the Astro CSP meta tag and `'unsafe-inline'`. Revised in part by D-65 on 2026-09-12: PR-15 sets `build.inlineStylesheets`, and PR-5 keeps the CSP header. The rest stands. Revised in part by D-90 on 2026-09-14: the CSP adds `font-src 'self'`. The rest of the CSP and `build.inlineStylesheets` stand. Revised in part by D-96 on 2026-09-14: `img-src` drops `data:`, because the icons of PR-8 replace the empty data URL of the placeholder favicon. The rest stands. |
| D-58 | 2026-09-12 | HSTS | `firebase.json` sends `Strict-Transport-Security: max-age=31536000; includeSubDomains`. No preload before M-3. | PR-5. On 2026-09-12 the GoDaddy parking host sends `max-age=63072000; includeSubDomains; preload`. With no configured value, Firebase sends `max-age=31556926` and no `includeSubDomains` on a custom domain (https://firebase.google.com/docs/hosting/full-config and decktome.com, read 2026-09-12). A preload removal takes months (https://hstspreload.org/removal/, read 2026-09-12). |
| D-59 | 2026-09-12 | Header check | After each preview deploy, the preview workflow fetches the preview URL and compares each header with `firebase.json`. A Playwright test loads the page and fails on any console error. | PR-5 and M-1. The site checks serve `dist/` from `astro preview` or a plain local server, and both ignore `firebase.json` (read 2026-09-12). A CSP block logs a console error. Firebase overwrites HSTS on `*.web.app`, so PR-6 checks HSTS on the live domain (https://firebase.google.com/docs/hosting/full-config, read 2026-09-12). The owner chose it over the Hosting emulator in the check jobs, and over live checks alone. Revised in part by D-80 on 2026-09-14: the console check drops the console error of the 404 status of the 404 page itself. The rest stands. |
| D-60 | 2026-09-12 | Agentic browsing in the budget | `lighthouse-budget.json` gets an `agentic-browsing` floor of 0.95, and the budget script reads that category like the other four. The change lands as its own pull request, PR-14. | Completes D-37 and D-50: "every category" includes the fifth category of Lighthouse 13.4.1 (the default config of the installed Lighthouse 13.4.1, read 2026-09-12). A later Lighthouse update can change the audits of the category. The owner chose it over a report with no floor, and over a narrower D-37. |
| D-61 | 2026-09-12 | Pinned actions setting | Turn on the GitHub setting "Require actions to be pinned to a full-length commit SHA" for this repository. | G-4 and D-16. The session changed the setting on 2026-09-12, and the API then read `sha_pinning_required: true`. Each current workflow already pins SHAs. Nobody checked yet whether the setting stops the Dependabot jobs that GitHub runs (GitHub docs, read 2026-09-12). |
| D-62 | 2026-09-12 | Workload Identity Federation pools | Each project has its own pool and provider. The provider of `natekramber-preview` trusts only pull request tokens of this repository. The provider of `natekramber-prod` serves the live deploy of PR-6 (D-63). | PR-5 and PR-6. Neither pool can issue credentials for the other project. Each condition names the numeric ids 1367643959 (repository) and 190805558 (owner), because Google Cloud advises against names (read 2026-09-12). The owner chose it over one shared pool in `natekramber-prod`, and over a third project for pools. |
| D-63 | 2026-09-12 | Live deploy lock | PR-6 deploys from a job in the GitHub environment `production`. The environment accepts the `main` branch alone, and no administrator can bypass it. The live service account trusts only the OIDC subject `repo:nkramber@190805558/portfolio@1367643959:environment:production`. | PR-6. Two separate locks: GitHub refuses a run outside `main`, and Google refuses a token outside the environment. A pull request runs on `refs/pull/N/merge`, so the branch rule stops it. The subject comes from the prefix that the GitHub API gives, and no real token confirms it yet (read 2026-09-12). The owner creates the environment before the workflow runs, because GitHub creates a missing environment with no protection. The owner chose it over a condition on `ref` alone, and over an environment with bypass. |
| D-64 | 2026-09-12 | Placeholder 404 page | PR-5 adds a minimal `src/pages/404.astro` in the style of the placeholder: one short line and a link home. PR-8 replaces the words. | PR-5. Firebase Hosting serves `404.html` from the public root, and it serves its own generic page when that file is absent (https://firebase.google.com/docs/hosting/full-config, read 2026-09-12). `dist/` holds no `404.html` on 2026-09-12. The header check of D-59 gets a real 404 response. The owner chose it over the default page of Firebase until PR-8. Revised in part by D-65 on 2026-09-12: PR-15 adds the page, not PR-5. The rest stands. Revised in part by D-98 on 2026-09-14: PR-8 keeps the words of D-74 and does not replace them. The rest stands. |
| D-65 | 2026-09-12 | PR-5 split | A new pull request, PR-15, ships the site code of PR-5 first: the stylesheet file of D-57 and the 404 page of D-64. PR-5 keeps the hosting setup and the previews. | Hard rule 3 and D-12. The order is the move of D-55, PR-14, PR-15, and then PR-5. The first preview of PR-5 then tests the CSP against the stylesheet file. The owner chose it over one PR-5. Revised in part by D-69 on 2026-09-13: no move comes before PR-14, so the order is PR-14, PR-15, and then PR-5. The rest stands. |
| D-66 | 2026-09-12 | Preview URL | A separate job of the preview workflow posts the preview URL and its expiry date as one pull request comment, and it updates that comment on each push. Only that job can write a comment, and the deploy job holds no write token. | PR-5 and M-1. The owner opens each preview on a phone (the gate of PR-8). The owner chose it over the job summary alone, and over an environment URL, whose button on the pull request page is unverified (GitHub docs, read 2026-09-12). |
| D-67 | 2026-09-12 | Preview channel lifetime | Each preview deploy sets `--expires 30d`. A job deletes the channel of a pull request when the pull request closes. | PR-5. A channel lasts 7 days by default and 30 days at most, and a redeploy restarts the time (https://firebase.google.com/docs/hosting/manage-hosting-resources, read 2026-09-12). No Firebase doc gives the channel limit of a site: a community pull request says 50, and a blog says 7 (read 2026-09-12). The owner chose it over 7 days with a cleanup job, and over the defaults alone. |
| D-68 | 2026-09-12 | Required preview check | The preview header check runs as the job `verify:site-preview`, and it joins the `main` ruleset after its first green run (D-11). | PR-5. A bad header or a broken CSP then blocks a merge. An outage of Firebase or Google Cloud also blocks a merge until it clears or the owner edits the ruleset. A fork pull request gets a read-only token, so the job skips there. Nobody checked yet how GitHub counts a skipped required job on a Dependabot pull request, so PR-5 checks it before the ruleset change. The owner chose it over a report that never blocks a merge. |
| D-69 | 2026-09-13 | Repository location, revised | The repository stays at `/Users/nate/Repos/portfolio`. It does not move to the external drive. | Supersedes D-55. The owner states that the move request was a miscommunication. No file moved, and no copy exists on `/Volumes/SSD-1TB`. The handoff drops the checklist of the move, and PR-14 comes next. |
| D-70 | 2026-09-13 | Foreground preview for the checks | `playwright.config.ts` sets `ASTRO_PREVIEW_BACKGROUND` for the web server of the checks, so `astro preview` stays in the foreground in an agent shell. The fix ships as its own pull request, PR-16. | PR-16. Astro 7.3.2 runs `astro preview` in the background when `am-i-vibing` detects an AI agent, and it skips that detection when `ASTRO_PREVIEW_BACKGROUND` is set (installed `dist/cli/preview/index.js`, read 2026-09-13). Playwright then failed with "Process from config.webServer exited early", and the detached server stayed on port 4321. CI sets no agent variable. Playwright 1.63.0 merges `webServer.env` over `process.env` (installed `lib/runner/index.js`, read 2026-09-13). The owner chose it over a change inside PR-14, and over a local variable with no code change. |
| D-71 | 2026-09-13 | Checks of the 404 page | The responsive tests, the axe scan, the HTML validation, and the link check load the 404 page. The Lighthouse budget stays on the home page. | PR-15. The scope of PR-15 named the responsive tests and the axe scan alone, and an exit test named every site check. D-48 caps the first mobile load, and that load is the home page. The link check started at the home page alone and scanned 1 link on 2026-09-13, so it now starts at every built HTML file. The owner chose it over all five checks, and over the responsive tests and the axe scan alone. |
| D-72 | 2026-09-13 | Inline style check | `make no-inline-style-check` fails when a built HTML file holds a style element or a style attribute. Its self-test proves that the check finds each defect in a planted fixture. The `verify:site` job runs the target. | PR-15. The CSP header of D-57 permits styles from files alone (`style-src 'self'`), so a browser blocks both forms of inline style. The job name stays the same, so the `main` ruleset does not change. The owner chose it over one check by hand in PR-15, with the console error test of D-59 as the later guard. |
| D-73 | 2026-09-13 | Placeholder layout | One layout component, `src/layouts/Placeholder.astro`, holds the head, the main landmark, and the styles of the home page and the 404 page. | PR-15. Tenet T-3 permits an abstraction after two concrete uses, and the two placeholder pages are those uses. PR-8 replaces the layout. The owner chose it over a copy of the styles in each page, and over a shared CSS file with a copy of the head in each page. |
| D-74 | 2026-09-13 | Placeholder 404 words | The 404 page has the title "Page not found \| Nate Kramber", the heading "There’s no page at this address.", and the link text "Go to the home page". | PR-15. D-64 stands: PR-8 replaces the words. The `copy-editor` agent reviews them (D-28). The owner chose them over "Page not found." with "Back to the home page", and over "That page doesn't exist." with "Go home". Revised in part by D-98 on 2026-09-14: PR-8 keeps these words. The rest stands. |
| D-75 | 2026-09-13 | Other security headers | `firebase.json` sends three headers besides the CSP of D-57 and the HSTS of D-58: `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, and `Cross-Origin-Opener-Policy: same-origin`. | PR-5. Every core browser supports each of the three since 2021 or earlier, and none can break this site (MDN browser compat data 8.1.1 and a Chromium 153 test, read 2026-09-13). `X-Frame-Options` stays out, because an enforced `frame-ancestors` makes browsers ignore it (CSP3 §6.4.2.2). `Permissions-Policy` and `Origin-Agent-Cluster` are Baseline Limited, so D-32 keeps them out. `X-XSS-Protection` is deprecated (MDN). The site needs no cross-origin isolation, so CORP and COEP stay out. The five security audits of Lighthouse 13.4.1 have weight 0 in Best Practices. The owner chose it over CORP and COEP too, over the full OWASP list, and over the CSP and HSTS alone. |
| D-76 | 2026-09-13 | Trusted Types | The CSP of D-57 gets no `require-trusted-types-for` directive. | D-57 stands. `default-src 'none'` already blocks every script, and Trusted Types guards only the DOM sinks that a script uses. Trusted Types is Baseline Newly available since 2026-02-24 (webstatus.dev, read 2026-09-13). A later decision that permits a script revisits it. The owner chose it over the directive in D-57 now. |
| D-77 | 2026-09-13 | Cache policy | `firebase.json` sends `Cache-Control: no-cache` on `**`, and then `max-age=31536000, immutable` on `/_astro/**`. | PR-5. With no rule, Firebase Hosting sends `max-age=3600`. A redeploy clears the CDN cache, but not the cache of a browser (https://firebase.google.com/docs/hosting/manage-cache, read 2026-09-13). The Firebase docs say only that matching rules apply in order. A later rule wins the same header on dart.dev, a Firebase site, and in the superstatic 9.2.0 emulator, and firebase-tools issue 8917 is open (read 2026-09-13). The header check of D-59 confirms the override on each preview. The owner chose it over the default of Firebase, and over `max-age=300` for HTML. |
| D-78 | 2026-09-13 | Service account addresses | The workflow files name each deploy service account address and each project number. A service account address and a project number are not personal data under hard rule 10. | PR-5 and PR-6. Each address has the form `NAME@PROJECT.iam.gserviceaccount.com` and names a machine account, not a person. The provider condition on the repository ids controls access, not the secrecy of these values (D-62). No repository variable or setting changes. Hard rule 10 stands for the email, phone number, and street address of a person. The owner chose it over GitHub repository variables. |
| D-79 | 2026-09-14 | Deploy setup runner | The session runs the setup commands of `docs/deploy.md` with the owner account. It stops only when the owner must act, for example a browser sign-in. | PR-5. The owner asked for it on 2026-09-14. It replaces the plan in `docs/design.md` that the owner runs each setup command. D-34 stands: the Spark plan and no billing account. D-52 stands: each command uses the gcloud configuration `natekramber`, and the session passes it to each command, so the active configuration of the owner does not change. |
| D-80 | 2026-09-14 | M-1 result | M-1 passes. The preview workflow deployed #10 to a channel of `natekramber-preview` through Workload Identity Federation, with no JSON key. The deploy service account needed `roles/firebasehosting.admin` alone, and the pool needed `roles/iam.workloadIdentityUser` on that account. | M-1 and PR-5. Run 34852763280 of 2026-09-14 UTC: `preview:deploy` returned `"status": "success"` with an expire time of 2026-10-14, and it logged no permission warning. The auth action removed its credentials file at the end of the job. API Keys Viewer was not necessary, and the issuer with no trailing slash works. The header check of D-59 passed on `/`, its stylesheet, and `/no-such-page`, and the stylesheet got the one-year cache of D-77. The console check failed on `/no-such-page`: Chrome logs a console error for the 404 status of the page itself, with the page URL as its source (local Chromium 153 run, 2026-09-14). D-64 requires that status, so the check now drops that one message for the page itself. A CSP error, or a 404 of any other file, still fails the check. Revises D-59 in part: the rest of D-59 stands. |
| D-81 | 2026-09-14 | Required preview check timing | `verify:site-preview` joins the `main` ruleset before the merge of #10, right after its first green run on `a3be515`. | Completes D-68. The session added the check at 2026-09-14 14:17 UTC and read the ruleset back: seven required checks, each from the GitHub Actions app (15368), and every other rule unchanged. #10 then read `CLEAN` and `MERGEABLE`. The owner chose it over an addition after the merge, and over a check that blocks no merge for a while. |
| D-82 | 2026-09-14 | Domain move | Firebase Hosting takes `natekramber.com` in the advanced order. The owner adds the ownership TXT record and both certificate challenge TXT records at GoDaddy first. The A records and the `www` CNAME change only after both domains read `CERT_ACTIVE`. | PR-6. `https://natekramber.com` sends `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` from GoDaddy (`curl -I`, 2026-09-14). A browser that stored that policy gives no way past a certificate error (RFC 6797, sections 8.4 and 12.1). The session created both custom domains through the Hosting API v1beta1, and the owner added the three TXT records on 2026-09-14. Both domains read `OWNERSHIP_ACTIVE` at 14:52 UTC. The owner chose it over a direct A record swap, and over the Firebase console. |
| D-83 | 2026-09-14 | GoDaddy site | The GoDaddy Website Builder site on `natekramber.com` goes away. The owner disconnects it at the second DNS visit if GoDaddy asks. | PR-6. The two A records serve a Website Builder "Launching Soon" page, not a parking page, and GoDaddy documents other parking addresses (https://www.godaddy.com/help/park-a-domain-registered-with-godaddy-23936, read 2026-09-14). The earlier facts that name a parking host are wrong. The owner cancels any paid plan of that site apart from this repository. The owner chose it over a move of the site to another hostname, and over a wait. |
| D-84 | 2026-09-14 | Manual redeploy | `.github/workflows/deploy.yml` also runs by hand (`workflow_dispatch`), besides each push to `main`. | PR-6. A run by hand gets the same OIDC subject as a push, because the job names the environment `production` (https://docs.github.com/en/actions/reference/security/oidc, read 2026-09-14). The branch rule of the environment refuses a run on any ref but `main`. The owner chose it over deploys on merge alone. |
| D-85 | 2026-09-14 | Environment setup | The session creates the environment `production` through the REST API, with a branch policy that accepts `main` alone. The owner clears the administrator bypass in the settings of the environment. | PR-6 and D-63. The session created the environment on 2026-09-14, and its policy lists `branch:main` alone. The owner cleared the bypass at 14:50 UTC, and the API then read `can_admins_bypass: false`. No create field sets the bypass, but the read response holds it, so the research claim of 2026-09-14 that no API reads it is wrong. The owner chose it over a setup in the web settings alone. |
| D-86 | 2026-09-14 | Second DNS visit | The owner makes the second DNS visit of D-82 at once, after both certificates read `CERT_ACTIVE`. The session gives each GoDaddy step in the conversation. | PR-6 and D-82. Both certificates read `CERT_ACTIVE` at 15:58 UTC, with the type `TEMPORARY` and the expiry 2026-12-13 (Hosting API v1beta1). Before the visit, the Firebase address `199.36.158.100` sent a valid certificate for each name. At 16:09 UTC, both GoDaddy name servers gave the records of step 14 in `docs/deploy.md`, and the three TXT records stayed. The steps come from the GoDaddy help pages 19239, 19237, 19210, and 32079 (read 2026-09-14). The A records had no lock at GoDaddy, so the owner removed no connection (D-83). Both domains read `HOST_ACTIVE` by 16:30 UTC. Then the owner opened both addresses on a phone with Wi-Fi off, and saw the placeholder page with no warning. The owner chose it over a later visit. |
| D-87 | 2026-09-14 | M-2 link method | The session enables the Cloud Logging link of M-2 through the Hosting API with the owner account. The call sets `cloudLoggingEnabled` in the config of the site `natekramber-prod`. | M-2 and D-36. The method `sites.updateConfig` needs `updateMask`, because a call with no mask changes `max_versions` alone (Hosting API v1beta1 discovery document, revision 20260830, read 2026-09-14). The call has no billing field. It returned HTTP 200 at 17:22 UTC on 2026-09-14, and the config then read `cloudLoggingEnabled: true` on a project with no billing account. A second call with `false` removes the link. No primary source confirms that the link works with no billing account, so the log entry of M-2 gives the answer. The question told the owner that Cloud Logging then keeps the IP address, the city, and the country of each visitor for 30 days. The owner chose it over the Link button of the Firebase console, and over a wait. |
| D-88 | 2026-09-14 | M-2 result | M-2 passes. The Spark project `natekramber-prod`, with no billing account, sends the Hosting request logs to Cloud Logging. The log entry of a test visit holds the request URL, the referrer, and the country. | M-2 and D-36. The session set the link at 17:22 UTC (D-87). The test visit of 17:28 UTC showed at 17:39 UTC in the log `firebasehosting.googleapis.com/webrequests`, with the resource type `firebase_domain`. Its `requestUrl` kept the query, its `referer` held the test value, and `remoteIpCountry` read `US`. The entry also holds `remoteIpCity` and the full IPv4 address. `gcloud billing projects describe` then read `False`, with no billing account. A pass unblocks PR-13. The M-2 entry of `docs/design.md` marks the claim of 2026-09-12 resolved. |
| D-89 | 2026-09-14 | OQ-5 preview page | The preview of D-42 is a private page on claude.ai that the owner alone can open. It loads the candidate fonts from Google Fonts. The site gets no code and no font file for a choice that the owner does not pick. | PR-7 and OQ-5. D-42 stands: a rendered page with three font pairings and three accent colors, in light and dark, at phone and desktop widths. The page lives outside the repository, so the CSP of the site does not apply to it. The site later self-hosts the picked fonts alone (D-27, D-90). The owner chose it over a page on the preview channel of PR-7, which a public pull request comment names. |
| D-90 | 2026-09-14 | Font CSP | The CSP of `firebase.json` adds `font-src 'self'`. Fonts load from the origin of the site alone. | PR-7. Revises D-57 in part: the rest of the CSP stands. `default-src 'none'` also blocks fonts, so a self-hosted font needs `font-src 'self'` (https://www.w3.org/TR/CSP3/, section 6.8.3, read 2026-09-13). The header check of D-59 compares the new value on each preview. The owner chose it over system fonts alone, which D-27 and D-42 exclude. |
| D-91 | 2026-09-14 | Fonts and accent pick | The site uses Atkinson Hyperlegible Next for text and Atkinson Hyperlegible Mono for tags, badges, and labels, both self-hosted. The accent is Blueprint cobalt: `#1F4FD1` in the light scheme and `#86A8FF` in the dark scheme. | Closes OQ-5 and completes D-42 for PR-7. The owner picked from the preview page of D-89 on 2026-09-14. The Latin variable WOFF2 files weigh 51,748 bytes together (Fontsource 5.3.0, read 2026-09-14), and both families use the SIL Open Font License 1.1. The accent reaches 6.49 to 1 on `#FAFAF8` and 8.21 to 1 on `#101010`, the grounds of the preview (WCAG 2.2 relative luminance). Both upstream repositories of the fonts are archived (GitHub, read 2026-09-14). The owner chose it over Instrument Sans with Martian Mono, over IBM Plex Sans with IBM Plex Mono, and over the accents Proof magenta and Pass emerald. |
| D-92 | 2026-09-14 | Font loading | Both Atkinson faces use `font-display: optional`. Each page preloads the text face. The mono face gets no preload until a page uses it. | PR-7 and D-91. `optional` waits 100 ms at most and never swaps, so a font causes no layout shift. A late font leaves that page view in the fallback face (https://web.dev/articles/font-best-practices, read 2026-09-14). A font preload needs `crossorigin`, even from the same origin (MDN, read 2026-09-14). The owner chose it over `swap`, which shows the text at once and shifts it when the font arrives. Revised in part by D-111 on 2026-09-14: the home page preloads the mono face while it shows project cards. The rest stands. |
| D-93 | 2026-09-14 | Phone check address | For each phone check, the session gives the owner the preview address with no typing: the link in the pull request comment, or the Mac clipboard after the owner agrees. | The `responsive-qa` skill and PR-7. On 2026-09-14 the owner typed the PR-7 preview address on an iPhone 16 Pro, and Firebase answered "Site Not Found". The address holds a lowercase L, and a look-alike with 1 or i returns that page with the status 404 (`curl`, 2026-09-14). The owner chose the Mac clipboard over the link in GitHub and over no phone check for PR-7, and the check then passed. |
| D-94 | 2026-09-14 | Bio method | The session asks the owner three or four short interview questions. It drafts two or three versions of the About text from the answers alone, and the owner picks one version and edits it. | OQ-3 and PR-8. Revises D-28 in part: the owner gives the facts and approves the words, and the session writes the drafts. The rest of D-28 stands: the plain and direct voice, the first person, and no image. A draft uses no fact that the owner did not give (G-11). OQ-3 stays open until the owner approves the words, and the About section stays off the page until then. The owner chose it over an About section that waits for a later pull request, and over a bio that the owner writes alone. |
| D-95 | 2026-09-14 | Motion | On load, the hero rises a few pixels into place in 500 ms. Only `translate` changes, and the text never fades. A link thickens its underline with a 150 ms transition on hover. Both run only under `prefers-reduced-motion: no-preference`. | PR-8 and D-27. Paint Timing counts an element as contentful only when its opacity is above 0, and the LCP algorithm skips text with an opacity of 0 (https://w3c.github.io/paint-timing/ and https://w3c.github.io/largest-contentful-paint/, read 2026-09-14). FCP and LCP carry 35 of the 100 points of the Lighthouse 13.4.1 performance score (installed `core/config/default-config.js`). WCAG 2.2.2 applies only to motion that starts automatically, lasts more than 5 seconds, and shows next to other content. WCAG 2.3.3 is level AAA (W3C Understanding docs, read 2026-09-14). W3C technique C39 permits motion that only `no-preference` turns on. The reduced-motion reset now also sets each delay to 0. The owner chose it over motion on hover and focus alone, and over no motion. |
| D-96 | 2026-09-14 | Favicon | The icon is a white N and K, drawn as strokes, on a rounded square in the accent `#1F4FD1`. `public/favicon.svg` holds it. `scripts/make-images.mjs` draws a 48 px PNG icon and a 180 px touch icon from that file, and both PNG files stay in git. | PR-8. Revises D-57 in part: D-57 kept `data:` for the placeholder favicon until PR-8, so `img-src` drops `data:`. The rest of the CSP stands. Google Search takes BMP, GIF, ICO, PNG, JPEG, PPM, and TIFF, and no SVG. It recommends an icon larger than 48 px (https://developers.google.com/search/docs/appearance/favicon-in-search, updated 2026-08-28, read 2026-09-14). Safari supports an SVG icon since 26.0 (https://webkit.org/blog/17333/, read 2026-09-14). The icon has its own background, so it looks the same in a light tab and a dark tab. The owner chose it over a plain cobalt square, and over letters that change color with the color scheme. |
| D-97 | 2026-09-14 | Share image | The share image is a 1200 by 630 PNG at `/og.png`. It shows the name, the headline of D-29, and a rule in the accent, in the text face and the light scheme. `scripts/make-images.mjs` draws it in the Chromium build of Playwright, and the PNG file stays in git. The home page sends the Open Graph tags and `twitter:card`. | PR-8 and D-41. LinkedIn asks for 1200 by 627 pixels or more, a 1.91:1 ratio, and 5 MB or less (https://www.linkedin.com/help/linkedin/answer/a521928, read 2026-09-14). Facebook recommends 1200 by 630 (https://developers.facebook.com/docs/sharing/webmasters/images/, read 2026-09-14). The X card docs moved, so the crop on X is unverified. sharp 0.35.4 is an optional dependency of Astro 7.3.2, and a build-time image takes its fonts from the build machine. A file in git keeps one address and the same bytes. The owner chose it over the name with the second line, and over the name alone. |
| D-98 | 2026-09-14 | 404 words | The 404 page keeps the words of D-74 as its final words: the title "Page not found \| Nate Kramber", the heading "There’s no page at this address.", and the link text "Go to the home page". | PR-8. Revises D-64 and D-74 in part: PR-8 keeps the words and does not replace them. The rest of both decisions stands. The GOV.UK pattern for a page-not-found page asks for no blame of the reader, no "404", and no "oops" (https://design-system.service.gov.uk/patterns/page-not-found-pages/, read 2026-09-14). The D-74 words meet each rule. The owner chose them over the D-74 words with the two address lines of that pattern, and over the full pattern. |
| D-99 | 2026-09-14 | Meta description | The meta description and `og:description` of the home page read: "I design the systems. AI agents write the code. I decide what ships. Tools and games, built under strict rules and real review." | PR-8, D-28, and D-29. The copy review of PR-8 found that the placeholder text was the only third-person text on the site, and that it reworded the approved headline. The new text uses the words of D-29 alone, in the first person. The share card then repeats the headline of the share image. The owner chose it over the third-person text of the placeholder. |
| D-100 | 2026-09-14 | Repeated profile links | The hero and the footer both keep the GitHub and LinkedIn links, also while the About section is empty. | PR-8 and D-20. D-20 names the hero and the links section. With About empty, the two lists sit about half a screen apart, and a screen reader hears each link twice. The responsive and the accessibility audits of PR-8 found no WCAG failure, and the accessibility audit advised to keep both. The footer copy gains value when the bio and the project cards push the footer down. The owner chose it over the footer list alone, and over the hero list alone, until the page grows. Superseded by D-102 on 2026-09-14. |
| D-101 | 2026-09-14 | Footer hairline | A 1 px line in `--color-rule` sits above the footer heading, aligned with the text column, in both color schemes. | PR-8. The responsive audit found the footer weak at 2560 px: a small heading and two links below a large empty area, under a 76 px headline. The line is decorative, so no contrast rule applies to it. The owner chose it over whitespace alone. Revised in part by D-102 on 2026-09-14: the footer and its line leave the page until the project cards arrive. The line comes back with the footer. |
| D-102 | 2026-09-14 | Links in the hero alone | The GitHub and LinkedIn links show in the hero alone for now. The Links section at the end of the page, with its heading and its line, leaves the page. It comes back when the project cards make the page longer. | PR-8. Supersedes D-100. Revises D-20 and D-21 in part: the Links section waits for the project cards, and the rest of both decisions stands. The owner checked the preview of #17 on a phone and saw the same links in two places, with nothing between them. The owner chose it over the hero list alone for good, over the Links section alone, and over both lists (D-100). |
| D-103 | 2026-09-14 | Fixture cards | The fixture cards of PR-9 come from a second build. `PORTFOLIO_FIXTURES=1` adds the entries of `tests/fixtures/projects/` to the collection, and the build goes to `dist-fixture/`. A second Playwright web server serves that folder on port 4322, inside the responsive and the accessibility runs. | PR-9. The production build in `dist/` never holds a fixture, and the schema checks the fixture entries too. Astro 7.3.2 accepts `--outDir` for `build` and `preview`, and `--ignore-lock` starts a second preview server in the foreground (installed `dist/cli/flags.js` and `dist/cli/preview/index.js`, read 2026-09-14). Playwright 1.63.0 accepts a list of web servers (installed `lib/common/index.js`, read 2026-09-14). No new CI job and no ruleset change. The Astro Container API is still experimental in 7.3.2. The owner chose it over the Container API, and over fixture tests that wait for a real card in PR-10. Each `PORTFOLIO_FIXTURES` value builds with its own `cacheDir`, because a build keeps its content store there (installed `dist/content/paths.js`). With one shared cache, a later site build reused the fixture entries and put them into `dist/` (Session 16, 2026-09-14). Revised in part by D-112 on 2026-09-14: a fixture build loads the fixture entries alone, and no site entry. The rest stands. |
| D-104 | 2026-09-14 | Card styles | The card component keeps its own scoped style block, so the home page loads a second small stylesheet. | PR-9, T-3, and T-4. With `build.inlineStylesheets: 'never'`, Astro puts CSS that several pages share into a separate chunk (https://docs.astro.build/en/guides/styling/, read 2026-09-14). The styles sit next to the markup, and T-3 ranks above T-4. The Lighthouse budget of D-37 and D-48 must still hold. The owner chose it over card rules in the layout, and over the undocumented Vite setting `cssCodeSplit: false`. |
| D-105 | 2026-09-14 | Statuses | The status badge allows two values: "Live" and "In development". | PR-9 and D-22. The schema refuses any other value, so a typo fails the build. Deck Tome uses "Live" (D-24), and What You Carry uses "In development" (PR-11). A new value waits for a project that uses it (T-3). The owner first selected "Live" alone, then added "In development" when the session quoted PR-11. |
| D-106 | 2026-09-14 | Card placeholder | A card with no screenshot shows a CSS panel: a 16:10 box in `--color-accent-tint`, with the project name in the mono face. The panel is decoration, so screen readers skip it. | PR-9 and D-40. The CSP of D-96 has no `data:` in `img-src`, so an inline image is not possible. The panel adds no request and follows the dark scheme through the tokens. When a screenshot arrives (OQ-4), the card shows the image instead. The owner chose it over one shared SVG file, and over a PNG for each project. |
| D-107 | 2026-09-14 | Card layout | A card always shows its title as a heading, the pitch, the status, the tags, and the links. A "Highlights" disclosure opens the highlights and the screenshot or the placeholder. | PR-9, D-22, and D-23. A link inside `summary` fails the axe rule `nested-interactive` (wcag2a, installed axe-core 4.13.0). MDN warns that some screen readers do not expose a heading inside `summary` as a heading (https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/summary, read 2026-09-14). The summary reads "Highlights", with the project name as visually hidden text. The owner chose it over a whole row that opens, and over a screenshot that always shows. |
| D-108 | 2026-09-14 | Tag outline | Each stack tag of a card gets a 1 px outline in `--color-rule`, with a little inline padding. | PR-9 and D-107. At 200% text on a 320 px screen, a long tag broke inside the word, and with a gap alone the two halves read as two tags (responsive audit of PR-9, 2026-09-14). The outline is decoration, so no contrast rule applies to it. The owner chose it over a middle dot between tags, which some screen readers read aloud, and over a gap alone. |
| D-109 | 2026-09-14 | Status badge place | The status badge follows the card title with a small gap, and it wraps under the title when the row is too narrow. | PR-9 and D-107. From about 1024 px up, the badge sat at the right edge of the column, up to 528 px from its title (responsive audit of PR-9, 2026-09-14). The owner chose it over the right edge. |
| D-110 | 2026-09-14 | Placeholder size | The placeholder panel of D-106 grows to 36rem wide at most, and it sits at the start of the column. A screenshot still uses the full column. | PR-9 and D-106. On a desktop, the panel filled the 920 px column at 575 px tall with one line of text, like an empty image slot (responsive audit of PR-9, 2026-09-14). The owner chose it over the full column width. |
| D-111 | 2026-09-14 | Mono face preload | The home page preloads the mono face while it shows project cards. The 404 page shows no mono text, so it preloads the text face alone. The text face preload comes first in the head. | PR-10. Revises D-92 in part: the mono face gets a preload on the page that shows it. The rest of D-92 stands. The rule in `.claude/rules/site.md` now reads "preload a face only on a page that shows it". The session measured the fixture build of PR-9 in Chromium build 1243, at a 390 by 844 phone viewport, with three runs of each case (2026-09-14). With no network throttling, the mono face rendered in 1 of 3 first visits with no preload, and in 3 of 3 with a preload. On the DevTools "Slow 4G" profile with a 4 times CPU slowdown, both faces used the fallback face in every run, with or without the preload. The home page fetches the 17,752-byte file in both cases. On Slow 4G, the preload moved the end of the text face download about 100 ms later, and the LCP stayed near 740 ms. The owner chose it over no preload, and over a preload on every page. |
| D-112 | 2026-09-14 | Fixture build scope | A build with `PORTFOLIO_FIXTURES` loads the entries of its test folder alone, and no entry of `src/content/projects/`. The home page tests open every card: the width check at each width, text at 200 percent, and the text spacing of WCAG 1.4.12. | PR-10. Revises D-103 in part: `PORTFOLIO_FIXTURES=1` no longer adds the fixture entries to the site entries. The rest of D-103 stands, and each value keeps its own content cache. With the first real entry, `fixture-screenshot.json` and Deck Tome both held order 1 (D-43), and the home page throws on a shared order number. The fixture tests also count exactly 2 cards. The fixture page now stays the same when a project joins the site. The owner chose it over fixture order numbers that no real card uses, such as 901 and 902, with a count of the fixture cards by id. |
| D-113 | 2026-09-14 | Deck Tome pitch | The pitch reads "Describe the deck you want, answer a few questions, and get a legal Magic: The Gathering deck." | PR-10 and D-22. The words come from line 3 of the decktome `README.md` on `main` (read 2026-09-14): "describe the deck you want, answer a few questions, and get a legal, validated deck with card art". The pitch has 94 characters and 17 words. The copy review preferred a first-person pitch that names the AI and the rules engine. The owner chose it over "I designed a Magic: The Gathering deck builder where AI picks the cards and a rules engine checks every one.", and over "I designed a Magic: The Gathering deck builder that asks before it builds and checks every card against the rules." |
| D-114 | 2026-09-14 | Deck Tome highlights | The card has three highlights, word for word: "A rules engine checks every card for legality on the day, color identity, power bracket, and ownership before you see the deck." Then "I direct AI coding agents. Every change goes on a branch, gets an automated code review and a full CI run, and waits for me to merge." Then "I keep a dated, public decision register: 721 decisions in the first 23 days." | PR-10 and G-11. The sources on decktome `main` (read 2026-09-14): guardrail 1 at line 452 of `docs/design-roadmap.md`, lines 43 to 66 of `CLAUDE.md`, and 721 unique decision ids in `docs/decisions.md`, from D-1 on 2026-08-23 to D-727 on 2026-09-14. The owner picked "Just the first three" of five candidates. Then the owner kept these words and refused two changes of the copy review: "The AI picks the cards" at the start of the first highlight with no "on the day", and "decision log" for "decision register". The owner chose it over five highlights, which added the deploy on merge and the free end-to-end test. |
| D-115 | 2026-09-14 | Deck Tome tags | The card has three stack tags: Go, TypeScript, and React. | PR-10 and D-22. Line 3 of the decktome `README.md` names Go, Protobuf, TypeScript, and React (read 2026-09-14). The owner wrote the list. The owner chose it over six tags with Protobuf, Firebase, and Cloud Run, over six tags with the two AI providers, and over four tags with Cloud Run. |
| D-116 | 2026-09-14 | Deck Tome links | The card has two links: "decktome.com" to `https://decktome.com/` with the note "Invite only", and "Source on GitHub" to `https://github.com/nkramber/decktome`. | PR-10 and D-24. The owner chose the labels of D-24 over "Live app" and "GitHub repository", and over a third link to a shared sample deck. |
| D-117 | 2026-09-14 | Hero height with cards | While the home page shows project cards, the hero stops 10rem short of the screen height, so the Projects heading shows at the bottom of the first screen. The 404 page keeps a hero of the full screen height. | PR-10, D-27, and PR-8. The responsive audit of PR-10 found that the hero of `100svh` hid every sign of the cards (2026-09-14). "Projects" started 24 px (phone) to 80 px (desktop) below the fold, under 270 to 314 px of empty space. A test checks the heading at five screen sizes. The owner chose it over the full-height hero. Revised in part by D-128 on 2026-09-15: the rule covers portrait screens, and the rest stands. |
| D-118 | 2026-09-14 | Footer weight | The heading of the Links section takes the body text size and the muted color. The footer adds no space above its hairline, because the Projects section already ends with its own space. The hairline of D-101 stays. | PR-10, D-101, and D-102. The responsive audit of PR-10 found that on a phone, the footer read as another card: a full-width line, then "Links" at the size of a card title (2026-09-14). On a desktop, 178 to 184 px of empty space sat above the line. The muted color meets 6.24 to 1 in light and 7.60 to 1 in dark. The owner chose it over a footer with no line, which revises D-101, and over the footer of PR-8 with no change. |
| D-119 | 2026-09-14 | Link note spacing | In a card, a link with a note has no bottom padding, and the gap between two links grows to `--space-l`. A note that moves under its link then sits close to that link. | PR-10, PR-9, and D-107. The change applies to every card (G-2). The responsive audit of PR-10 found that at 200 percent text on a 320 px screen, "Invite only" sat 40 px below its link and 48 px above the next link (2026-09-14). A test now requires the gap to the own link to be at most half the gap to the next link. The link stays a target above 24 CSS pixels, and the row gap keeps its focus ring off the note. The owner chose it over the card with no change. |
| D-120 | 2026-09-14 | PR-10 phone check | PR-10 gets no phone check by the owner. | PR-10 and the `responsive-qa` skill. The owner skipped the check when the preview of #19 went live. The exit tests of PR-10 name no phone check. Every check of #19 passed on `2948512`, and the responsive and the accessibility audits covered the layout. The session sent after-fix screenshots at 390 px, 1440 px, and 320 px with 200 percent text. The owner chose it over the Mac clipboard of D-93, and over the link in the pull request comment. |
| D-121 | 2026-09-14 | Card link names | Each card link holds the project name as visually hidden text after its label. The visible label does not change. | PR-11, D-107, and G-2. D-122 sets the form of the hidden text. The accessibility audit of PR-10 found that a second card gives two links the same name, "Source on GitHub" (WCAG 2.4.9, level AAA). WCAG 2.4.4 is level A. Its sufficient techniques include C7, CSS that hides a part of the link text. H80, the link text with the heading before it, is an advisory technique alone (https://www.w3.org/WAI/WCAG22/Understanding/link-purpose-in-context.html, updated 2026-05-18, read 2026-09-15). So the hidden name gives each card link a sufficient technique for 2.4.4, and it meets 2.4.9. The name starts with the visible label, the best practice of WCAG 2.5.3 (https://www.w3.org/WAI/WCAG22/Understanding/label-in-name.html, read 2026-09-15). The change applies to every card, with the same pattern as the toggle of D-107. A test checks the name of each link of the fixture cards. The question told the owner that 2.4.4 is level AA and that the card heading satisfies it. Both claims were wrong, and the session told the owner. The owner chose it over the same fix in a separate pull request, over a different visible label on the What You Carry card, and over no change. |
| D-122 | 2026-09-15 | Card link name form | The hidden text of D-121 starts with a space and holds the project name in parentheses. A screen reader names the link "Source on GitHub (What You Carry)". | PR-11 and D-121. A hidden span that starts with a comma gave the name "Source on GitHub , Deck Tome", with a space before the comma. The Chromium 153 accessibility tree and the name code of Playwright 1.63.0 gave the same result (scratch page, 2026-09-15). The span is absolutely positioned, so the name code reads it as a block and adds a space before it. Hidden text that starts with a space gave a clean name in both, as in the toggle name "Highlights of Deck Tome" of D-107. The owner chose it over a comma through `aria-label`, over the word "for", and over the comma with the extra space. |
| D-123 | 2026-09-15 | What You Carry pitch | The pitch reads "Descend a collapsed mine and decide at every stairwell: risk the gear you carry, or climb out and keep it." | PR-11, D-22, and D-25. The sources on What You Carry `main` at `a4bf6d6` (read 2026-09-15): `docs/design.md` line 29 reads "At each stairwell the player decides: descend or ascend. Ascension banks the gear. Death loses the gear that the player carried in and found on the way." Line 133 gives the biome, "a collapsed deep mine". The pitch has 106 characters and 20 words. It describes the design: the build has no enemies, no HUD, and no sound (`README.md` line 56), and the badge "In development" shows that. Line 3 of that README names Steam, so the pitch names no platform (D-25). The owner chose it over "A dungeon crawler where every stairwell asks one question: go deeper, or climb out and keep what you carry.", and over "Fight your way down a collapsed mine, and bank your gear before a death takes it." |
| D-124 | 2026-09-15 | What You Carry tags | The card has three stack tags: C#, Godot, and .NET. | PR-11 and D-22. The sources on What You Carry `main` at `a4bf6d6` (read 2026-09-15): `Directory.Build.props` line 4 sets C# 14, and line 1 of `WhatYouCarry.Game/WhatYouCarry.Game.csproj` names `Godot.NET.Sdk/4.7.2`. `global.json` pins the .NET SDK 10.0.400. The game rules live in `WhatYouCarry.Core`, a library with no engine dependency (`CLAUDE.md` line 71). The owner chose it over C# and Godot alone, and over four tags with xUnit. |
| D-125 | 2026-09-15 | What You Carry highlights | The card has two highlights, word for word: "Every change runs the game simulation on three operating systems, and the results must match bit for bit." Then "Every night, bots play 10,000 runs, and a sweep of 100,000 seeds checks that every chamber can be reached." | PR-11 and G-11. The sources on What You Carry `main` at `a4bf6d6` (read 2026-09-15): `.github/workflows/bit-identity.yml` runs on each pull request and each push to `main`. It has a Linux, a Windows, and a macOS job, and a job that compares their three hashes. Line 137 of `CLAUDE.md` asks for that job to pass before a merge. `.github/workflows/night.yml` runs each night at 08:07 UTC. Two bots play 5,000 seeds each, and a test sweeps 100,000 seeds for chamber reachability (lines 6, 16, and 32 to 39, and `docs/design.md` line 168). The first highlight names no operating system, so it cannot read as a platform plan (D-25). The owner picked two of four candidates, so the card has no highlight about the AI coding agents. The owner chose it over a review of each code change by the other AI coding agent, and over the exact replay of a run. Revised in part by D-126 on 2026-09-15: the second highlight uses plain words, and the first highlight stands. |
| D-126 | 2026-09-15 | What You Carry highlight words | The second highlight reads "Every night, bots play 10,000 runs, and a test builds 100,000 floors to check that every chamber can be reached." | PR-11 and D-125. Revises D-125 in part: the first highlight stands. The copy review of PR-11 found that "a sweep of 100,000 seeds" is too hard for a visitor who does not code or play games (D-19). On What You Carry `main` at `a4bf6d6`, `WhatYouCarry.Tests/ProcgenTests.cs` builds one floor plan for each seed (line 116). Its one sweep "digs each floor once", and two tests read that sweep (lines 98 to 100, 211, and 223). The night run gives the sweep 100,000 seeds (`night.yml` lines 6 and 36 to 39). The review read that file in the working tree, and the session read it again on `main`, with no difference. The new text has 112 characters. The owner chose it over the words of D-125. |
| D-127 | 2026-09-15 | Badge wrap at 200 percent text | The status badge "In development" keeps its wrap at 200 percent text on a narrow screen. PR-11 does not change the badge. | PR-11, D-105, and D-109. The responsive audit of PR-11 found that at 320 to 360 px with text at 200 percent, the badge runs to two lines, "In" and "development". At 320 px with 200 percent text and the spacing of WCAG 1.4.12, it runs to three lines and breaks inside "development". The rule `overflow-wrap: anywhere` of the badge permits that break, so the badge stays inside its 240 px column and the page gets no sideways scroll (G-1). No content is lost. PR-10 accepted the same break of "decktome.com" in the same mode. The owner chose it over a CSS change in PR-11 that keeps the word whole. |
| D-128 | 2026-09-15 | First screen in landscape | The Projects heading of D-117 shows on the first screen of a portrait screen. On a landscape phone, the heading can sit below the first screen. | PR-11 and D-117. Revises D-117 in part: the rule covers portrait screens, and the rest stands. The responsive audit of PR-11 found the bottom of the heading at 444 px on a 568 by 320 screen, at 549 px on 844 by 390, and at 582 px on 932 by 430. The same layout came with PR-10, and the test of D-117 checks five portrait sizes alone. The owner chose it over a landscape rule for the hero in a separate pull request, and over the same rule in PR-11. |
| D-129 | 2026-09-16 | PR-11 hand checks | PR-11 gets no phone check and no VoiceOver check by the owner. | PR-11, D-93, and D-120. Every check of #22 passed on `f6fd79f`, and the Gitar pause note held an approval with no finding. The responsive audit and the accessibility audit covered the layout and WCAG 2.2 level AA in Chromium 153. The preview channel of #22 expires on 2026-10-16. The VoiceOver check of the link names and of the status line stays in the handoff for a later session. The owner chose it over the Mac clipboard of D-93, and over the link in the pull request comment. |
| D-130 | 2026-09-16 | LinkedIn in the link check | The weekly link check skips every `linkedin.com` address, and a comment gives the reason. | PR-12 and D-20. On 2026-09-16, `https://www.linkedin.com/in/nate-kramber` answered 999 to a GET with the default `curl` agent and with a browser agent, and 405 to a HEAD with a browser agent. The LinkedIn `robots.txt` reads "The use of robots or other automated means to access LinkedIn without the express permission of LinkedIn is strictly prohibited." A removed profile answers 999 too, so an accepted 999 proves nothing about the link. linkinator 8.1.0 has the flag `--status-code "CODE:ACTION"` for that other route. The owner chose the skip over `999:ok`, and over `999:warn`. |
| D-131 | 2026-09-16 | Link check target | The weekly check crawls the live site `https://natekramber.com`. | PR-12 and D-16. The check then reads the links that a visitor follows, so a link that a bad deploy broke also shows. An outage of Firebase Hosting or of the domain fails the run. linkinator follows only the links of the same root domain, so it reads each outbound address once. The owner chose it over a check of the built files in `dist/`. |
| D-132 | 2026-09-16 | Link check failure signal | A dead link fails the workflow run. The workflow opens no issue, and it writes no run summary. | PR-12. GitHub tells the owner about a failed scheduled run, and the Actions tab shows the failure. The job keeps `permissions: contents: read`, so it needs no `issues: write` token. The session set the schedule to 09:17 UTC each Monday, because GitHub delays the schedule event at the start of an hour (https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows, read 2026-09-16). The owner chose it over an issue for each dead link, and over a run that always passes and writes a summary. |
| D-133 | 2026-09-16 | Card shape | A card holds no highlights and no disclosure. It shows the title, the pitch, the status, the tags, the links, an optional logo beside the title, and an optional screenshot after the links. | PR-17 and G-2. Supersedes D-107. Revises D-22 and D-23 in part: the field list of D-22 loses the highlights, and a card no longer expands in place. The order number of D-23 stands. The highlight words of D-114, D-125, and D-126 leave the site with the field. PR-12 holds D-130 to D-132, so this branch starts at D-133. The owner chose it over a flat card with a screenshot and no logo, over a disclosure that opens the screenshot alone, and over a logo with no screenshot. |
| D-134 | 2026-09-16 | A card with no image | A card with no logo and no screenshot shows no image. | PR-17 and OQ-4. Supersedes D-106 and D-110: the placeholder panel leaves the site. A card then ends after its links until the owner gives an image. The first mobile load keeps its room under the cap of D-48, because the page adds no image byte before an image exists. The owner chose it over the tinted panel of D-106, and over the logo as the image until a screenshot arrives. |
| D-135 | 2026-09-16 | The AI workflow on a card | No card describes how the owner directs AI coding agents. The headline of D-29 keeps that claim for the site. | PR-17, D-26, and D-114. Revises D-26 in part: the site copy can still describe the workflow, and no card does it now. The Deck Tome highlight of D-114 leaves the site with the highlights of D-133. The owner chose it over the About bio as the new place for it, and over a one-line note field on each card. |
| D-136 | 2026-09-16 | The Links section | The Links section at the end of the home page leaves the site. The GitHub and LinkedIn links show in the hero alone. | PR-17. Supersedes D-102, and with it D-101 and D-118: the footer, its hairline, and its heading weight leave the page. Revises D-20 and D-21 in part: the site keeps both profile links, and the hero alone holds them. The owner asked for the change on 2026-09-16, after the two cards made the page long. |
| D-137 | 2026-09-16 | The end of the page | The Projects section keeps at least 4rem of space below its last card, at every width. | PR-17 and D-136. The responsive audit of PR-17 measured 23.5 to 25.8 px below the last card box at 320 to 430 px. The gap from a link to the hairline of the next card is 40 px, so the end of the page read as the start of another card. The rule `padding-block-end: max(4rem, var(--space-section))` leaves the wide screens as they are. The owner chose it over a hairline below the last card, and over no change. |
| D-138 | 2026-09-16 | A tall screenshot | A card draws each screenshot in a 16 by 10 frame, and it shows the top of a taller image. | PR-17, D-133, and OQ-4. The responsive audit found no height cap on the screenshot, so an 800 by 4000 capture would draw about 920 by 4600 px on a desktop. The rules `aspect-ratio: 16 / 10`, `object-fit: cover`, and `object-position: top` give every card the same frame, and the frame holds the space before the image arrives. A capture wider than 16 by 10, for example 1200 by 630, loses a part of its sides, so the owner crops each capture first. The owner chose it over a height cap with empty space at the sides, and over no cap. |
