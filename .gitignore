# OS and editor files
.DS_Store
.vscode/
.idea/

# Node dependencies, the Astro build, and generated Astro types
node_modules/
dist/
dist-fixture/
.astro/
npm-debug.log*

# Test output: Playwright results and reports, and the Lighthouse fixture
test-results/
playwright-report/

# The Firebase CLI: the Hosting upload cache of a deploy, and the debug logs
.firebase/
firebase-debug.log
firebase-debug.*.log

# The harness: the worktree of a parallel session, and the local settings file
.claude/worktrees/
.claude/settings.local.json
